import React from 'react'

function Square({value}) {
    return (
        <div>
            <button className = "btn">
                {value}
            </button>
        </div>
       
    )
}

export default Square;
